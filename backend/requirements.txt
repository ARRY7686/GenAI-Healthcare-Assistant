fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3

# Dev / test
pytest>=8.0
httpx>=0.27          # required by fastapi.testclient.TestClient

# Real LLM provider (OPTIONAL — the app defaults to the offline mock):
#   Google Gemini -> pip install google-generativeai
#   then set TRIAGE_LLM_PROVIDER=gemini and TRIAGE_GEMINI_API_KEY in .env
