fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3

# Dev / test
pytest>=8.0
httpx>=0.27          # required by fastapi.testclient.TestClient

# NOTE: real LLM providers are an OPTIONAL follow-up (the app defaults to the offline mock).
#   anthropic -> pip install "anthropic>=0.40"
